export const selectProducts = (state) => state.products.products;
export const isLoadingProducts = (state) => state.products.isLoading;
export const errorProducts = (state) => state.products.error;
export const selectFavoritesProducts = (state) => state.products.favorites;
